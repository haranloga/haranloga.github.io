<% meta() %>
<%
    const posts = metas("./posts").filter(p => p.meta.published);
    posts.sort((a, b) => b.meta.date.localeCompare(a.meta.date));
    const base = url;
    const today = new Date().toISOString().slice(0, 10);
%><?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
<url><loc><%= base %>/</loc><lastmod><%= today %></lastmod><changefreq>weekly</changefreq><priority>1.0</priority></url>
<url><loc><%= base %>/projects/</loc><lastmod><%= today %></lastmod><changefreq>weekly</changefreq><priority>0.8</priority></url>
<url><loc><%= base %>/tags/</loc><lastmod><%= today %></lastmod><changefreq>weekly</changefreq><priority>0.6</priority></url>
<% for (const p of posts) { %><url><loc><%= base %>/posts/<%= p.directory %>/</loc><lastmod><%= p.meta.date %></lastmod><changefreq>monthly</changefreq><priority>0.7</priority></url>
<% } %></urlset>
